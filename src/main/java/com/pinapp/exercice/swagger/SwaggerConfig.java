package com.pinapp.exercice.swagger;


//@Configuration
//@EnableSwagger2
public class SwaggerConfig {
/*
    @Bean
    public Docket apiDocket() {
        return new Docket(DocumentationType.SWAGGER_2)
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.pinapp.exercice.controller"))
                .paths(PathSelectors.any())
                .build()
                .apiInfo(getApiInfo()).useDefaultResponseMessages(false);
    }

    private ApiInfo getApiInfo() {
        return new ApiInfo(
                "Customer Service API",
                "Customer Service API Description",
                "1.0",
                "http://localhost:8080/swagger-ui.html",
                new Contact("customers", "http://www.pinapp-development.com", "example@example.com"),
                "LICENSE",
                "LICENSE URL",
                Collections.emptyList()
        );
    }*/
}
